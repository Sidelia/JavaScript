/*Construa um programa que leia dois números e pergunte ao
usuário qual operação ele deseja executar: soma, subtração,
multiplicação ou divisão. Após a operação o programa deve exibir o
resultado do processamento.*/

let a = parseInt(window.prompt("Qual operação você deseja realizar:"));
operacao = 0;

switch (soma) {
    case 1:
    soma = x + y;
    window.prompt("Digite 2 valores:" {x, y});
    window.alert("A soma dos valores digitados é:" {soma});
    break;

    case 2:
    sub = x-y;
    window.prompt("Digite 2 valores:" {x, y});
    window.alert("A sub dos valores digitados é:" {sub});
    break;

    case 3:
    div = x / y;
    window.prompt("Digite 2 valores:" {x, y});
    window.alert("A divisão dos valores digitados é:" {div});
    break;

    case 4:
    mult = x * y;
    window.prompt("Digite 2 valores:" {x, y});
    window.alert("A multiplicação dos valores digitados é:" {mult});
    break;

    
}
alert ("O resultado é: " +operacao.tolocaleString('pt-BR', {style: 'currency', currency: 'BRL'}));
